node_modules/
dist/
src-tauri/target/
# regenerated by tauri on every build, ~300 KB of noise in diffs
src-tauri/gen/schemas/
*.log
.DS_Store
Thumbs.db

# built binaries — the release page carries these, not the repo
PlaySave.exe
*.exe

# scratch from testing the copy feature
test/
